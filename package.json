{
  "name": "collab-whiteboard",
  "private": true,
  "version": "1.0.0",
  "description": "Real-time collaborative drawing board (React + Node + Socket.IO, no database)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
